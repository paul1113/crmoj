<%= form_for(@departmentlist) do |f| %>
  <% if @departmentlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@departmentlist.errors.count, "error") %> prohibited this departmentlist from being saved:</h2>

      <ul>
      <% @departmentlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %><br>
    <%= f.text_field :uuid %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
