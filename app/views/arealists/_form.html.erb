<%= form_for(@arealist) do |f| %>
  <% if @arealist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arealist.errors.count, "error") %> prohibited this arealist from being saved:</h2>

      <ul>
      <% @arealist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %><br>
    <%= f.text_field :uuid %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
