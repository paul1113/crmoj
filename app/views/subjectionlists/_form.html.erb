<%= form_for(@subjectionlist) do |f| %>
  <% if @subjectionlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subjectionlist.errors.count, "error") %> prohibited this subjectionlist from being saved:</h2>

      <ul>
      <% @subjectionlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %><br>
    <%= f.text_field :uuid %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
