<%= form_for(@phone_type_list) do |f| %>
  <% if @phone_type_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_type_list.errors.count, "error") %> prohibited this phone_type_list from being saved:</h2>

      <ul>
      <% @phone_type_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %><br>
    <%= f.text_field :uuid %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
